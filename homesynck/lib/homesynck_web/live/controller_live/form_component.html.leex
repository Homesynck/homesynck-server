<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
  id: "controller-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save" %>

  <%= label f, :name %>
  <%= text_input f, :name %>
  <%= error_tag f, :name %>

  <%= label f, :last_online %>
  <%= date_select f, :last_online %>
  <%= error_tag f, :last_online %>

  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
