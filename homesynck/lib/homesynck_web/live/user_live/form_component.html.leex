<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
  id: "user-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save" %>

  <%= label f, :name %>
  <%= text_input f, :name %>
  <%= error_tag f, :name %>

  <%= label f, :email %>
  <%= text_input f, :email %>
  <%= error_tag f, :email %>

  <%= label f, :password_hashed %>
  <%= text_input f, :password_hashed %>
  <%= error_tag f, :password_hashed %>

  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
